language: node_js
node_js:
  - node

services:
  - postgresql

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - psql -c travis_ci_test < Schemas/users.sql >


  
script: "npm run-script test8"
    
after_script: "cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js"
